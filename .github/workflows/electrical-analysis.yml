name: Electrical analysis

on:
  workflow_dispatch:
    inputs:
      project_id:
        description: Registered board or schematic with reviewed electrical requirements
        required: true
        type: string

permissions:
  contents: read

jobs:
  run:
    uses: sheepfling/KiCad-Tooling/.github/workflows/electrical-analysis.yml@683cd567942ae8bc1a4f753cf0d259808fe1ee9f
    with:
      project_id: ${{ inputs.project_id }}
